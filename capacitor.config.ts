import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'position_setting_sample',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
